# Performance worklog (high level)

| Date       | Notes |
|------------|-------|
| 2026-04-25 | Shop: `ShopListProduct` payload + server facet summary; debounced search URL; paginated grid; perf/asset CI + `perf/budgets.json`; cache headers; icon pipeline `images:icons`; CSS reveal sections (no framer on reveal hooks); GA `lazyOnload`; DM Sans subset trimmed (kept 300 for `font-light`). |
